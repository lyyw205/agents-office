# Server
PORT=3001
NODE_ENV=development

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Agent Bridge
MAX_CONCURRENT_AGENTS=3
TASK_TIMEOUT_MS=300000

# Project Sync — comma-separated paths to scan for external projects
# Each path is scanned 1-level deep for directories containing team-config.json
SCAN_PATHS=~/repos
