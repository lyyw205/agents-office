{
  "name": "agents-office",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "dashboard",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n dashboard,server -c blue,green \"npm run dev -w dashboard\" \"npm run dev -w server\"",
    "build": "npm run build -w server && npm run build -w dashboard",
    "db:seed": "npm run db:seed -w server",
    "db:migrate": "npm run db:migrate -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
